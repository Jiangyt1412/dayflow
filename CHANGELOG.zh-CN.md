# 更新记录

[English](CHANGELOG.md) · [打开 DayFlow](https://jiangyt1412.github.io/dayflow/)

## 1.4.1 — 2026-09-24

- 移除 Hygiene 图表右上角的 Daily／Weekly／Monthly 分组选择，只保留 Insights 顶部的日期范围选择。Personal care 图表显示所选范围内的每日次数。
- Insights 横向拖动时内容随手指移动；短滑动、取消手势和首尾越界滑动会回弹。完成滑动后，以滑出淡化和缓动进入衔接页面；拖动过程中不逐帧重新渲染图表。保留纵向滚动、图表与输入控件操作、屏幕边缘手势和“减少动态效果”支持。
- 去掉 Today 的 Personal care 卡片内重复的历史图标。页面顶部的 Record history 入口仍可查看和编辑历史记录。
- 不修改已存数据、备份格式、专注计时、App 图标或振动行为。

### 验证

148 项单元／集成测试（15 个文件）和全部 34 项生产浏览器测试通过（浏览器套件用时 1.9 分钟）。TypeScript 与根路径生产构建通过；`/dayflow/` 部署构建通过，包含 35 项预缓存资源。尚未测量真实 iPhone 上的动画手感或帧率。

## 1.4.0 — 2026-09-24

- Insights 默认打开 **Focus**，后面依次为 **Sleep、Meals、Hygiene、Tasks、Notes**。可在普通内容区域左右滑动切换，也保留点击标签。切换时保留分区筛选与 Notes 未保存草稿；控件、图表、横向滚动和屏幕边缘的浏览器手势保留各自行为，过渡动画遵循“减少动态效果”。
- 新增 **Focus → Recent sessions → Add record**，用于补录遗漏的专注。点击已有记录可修改开始日期与时间、专注小时／分钟／秒、分类、关联任务和笔记；可以关联已完成任务。点顶部 **Save** 才保存，关闭不会提交；专注笔记不再仅因离开输入框而自动保存。
- 补录及修正时间按一个连续活动片段保存。修改已有开始时间或时长会移除原始暂停，编辑器会提前提示；仅修改分类、任务或笔记时，原始片段、暂停及毫秒精度保持不变。新增或修正时间后的记录必须大于零，结束时间不得晚于当前时间。
- 有名称快照时保留已删除任务名称。其他页面更改或删除同一记录后，拒绝当前过期的保存或删除操作。记录变更会更新任务累计与 Insights，不改动当前活动计时器。

### 验证说明

15 个文件中的 148 项单元／集成测试及全部 32 项生产浏览器测试通过，覆盖专注补录、时间修正、仅改文字等信息时保留时间精度、过期保存／删除保护、活动计时器隔离、滑动导航、草稿与筛选保留、手势避让及窄屏布局。TypeScript、根路径生产构建及 `/dayflow/` 部署构建通过；部署构建包含 35 项预缓存资源。另在独立开发演示数据中以 390px 和 320px 查看布局；桌面检查不等同于 iPhone 真机验证。本次不更改数据库标识或备份格式。详见 [QA.md](QA.md)。

## 1.3.1 — 2026-09-17

- 点击 Focus 分类扇区或引导线标签，直接以短暂淡入淡出和平移过渡进入该分类的任务饼图。每项任务仍通过引导线连接名称、用时和在分类中的占比，替代旧版的扇区高亮和下方任务列表。
- 点击 **All categories** 或按 Escape 返回。键盘操作返回时，焦点回到原分类标签；系统开启“减少动态效果”时跳过过渡。更改日期范围或分类筛选会返回分类饼图。
- Focus 图表标签、坐标、工具提示和数据表中，达到一小时的时长改为小时加分钟，例如 **3h 40m**；较短时长显示整分钟，正数但不足一分钟显示 **<1 min**。仅显示时省略不足一分钟的部分；保存的时间戳、饼图占比和 CSV 数值保留精度，以分钟填写的输入框不变。
- 修复多段短专注按小数分钟相加后，可能因浮点误差将整一分钟显示为不足一分钟的边界问题。分类和任务用时先累计毫秒，再换算分钟。
- 移除 Tasks 列表第一个任务上方的灰色分隔线；任务之间的分隔线保留，删除首项及 Undo 恢复后也保持正确。

### 验证说明

14 个文件中的 119 项单元／集成测试及全部 25 项生产浏览器测试通过，覆盖任务饼图与返回、引导线标签与占比、日期范围切换、减少动态效果、时长边界及删除／撤销后的首行样式。TypeScript 与生产构建通过。另在独立开发演示数据中以 390px 和 320px 查看布局；桌面检查不等同于 iPhone 真机验证。本次不改变数据库标识或备份格式。详见 [QA.md](QA.md)。

## 1.3.0 — 2026-09-17

- 手机底部导航增加浅绿色滑动选中背景。
- 任务、生活记录、分类和日记保存时显示 Saving / Saved；专注计时按钮显示正在执行的操作，并增加播放／暂停图标的短暂过渡。写入成功后才提示保存成功。
- 编辑弹窗增加短暂进入／退出动画；系统启用“减少动态效果”时关闭这些动画及导航滑动。
- 点击 Focus 饼图扇区或引导线标签时，同时突出扇区、引导线和标签。下方列出该分类各任务在所选日期范围内的专注分钟数，以及分类总时长与占比。点击图表空白、Clear selection 或按 Escape 恢复全部。
- 完成、重新打开或删除任务后，提供最近一次操作的 10 秒 Undo（撤销）。在当前 App 内切换页面仍可撤销。撤销删除会恢复任务、计划及符合条件的专注关联，不覆盖专注时长和笔记。导入备份或清空数据会使撤销失效；刷新页面不保留撤销提示。
- 任务左划只露出 Delete 按钮，即使划到底也不会直接删除；仍需再点 Delete。保留纵向滚动及编辑器中的删除入口。

### 数据说明

饼图任务明细与饼图使用相同的日期范围，按已结束专注的实际活动时间段统计，排除暂停。未关联任务的专注单独显示。已删除任务在有名称快照时保留名称；只有名称、没有任务 ID 的旧关联按名称合并，因此无法可靠区分同名的已删除任务。本次不迁移数据库或备份格式，临时撤销标记不会进入 JSON 导出。

### 验证说明

95 项单元／集成测试及全部 24 项生产浏览器测试通过，覆盖动画结束前持久化、原生表单验证、导航与减少动态效果、跨页面撤销、计划与专注保留、左划露出删除、饼图选择、范围内任务统计、键盘取消选择和窄屏布局。TypeScript 与生产构建通过。浏览器检查和桌面手机宽度预览不能替代 iPhone 真机手势或振动验证。详见 [QA.md](QA.md)。

## 1.2.1 — 2026-09-16

- 移除点击图表外层和内部 SVG 图层时出现的浏览器蓝色焦点框；保留键盘焦点提示与方向键图表操作。
- Sleep 和 Meals 的时间轴改为从上到下由早到晚。跨午夜睡眠保持连续时间顺序；时长、次数图的数值方向不变。
- 增加 Sleep、Meals、Hygiene、Tasks 两轴刻度文字之间的间距。
- Focus 分类饼图可独立选择近 7 天（默认）、近 30 天、近 90 天、全部记录或自定义起止日期；自定义范围包含起止两天。保留分类筛选与扇区引导线标签。
- 饼图日期范围只影响饼图，今日／本周／本月／全部累计以及月度小时图、逐日图、年度趋势的统计范围不变。

### 验证说明

87 项单元／集成测试及全部 19 项生产浏览器测试通过，覆盖触摸与键盘图表焦点、跨夜顺序、320／390／768px 坐标间距、日期范围统计、无效日期和窄屏日期控件。TypeScript 与生产构建通过。桌面浏览器检查不等同于 iPhone 真机验证。本次不更改数据库或备份格式。详见 [QA.md](QA.md)。

## 1.2.0 — 2026-09-16

- Insights 第一项改为 Notes，可按日期记录日记、心情和状态，保存在本机并纳入 JSON 备份。
- Focus 移至第二项，移除原 Overview。
- 重做专注统计：今日、本周、本月和全部累计时长；以引导线直接连接分类名称、时长与扇区的饼图；可横向滚动的小时分布；本月逐日和全年 1–12 月折线图。
- 图表按已保存的实际专注片段分配到本地日期和小时，排除暂停；未结束的计时暂不计入。小时分布不再设置最低记录数量门槛。
- 专注历史和详情显示任务名称。新记录保存名称快照；删除仍存在的任务时，也为旧专注记录补存名称。
- 区分进食总记录次数与选定期间的每日平均；平均值包含没有记录的日期。
- 移除非交互页面容器的浏览器焦点外框，折叠按钮的点击高亮设为透明，保留按钮和输入框的键盘焦点提示。尚未复现用户反馈的 iPhone 偶发蓝线，不能确认其唯一来源。
- 新增按版本记录的更新日志，Settings → What’s new 可查看。

### 备份兼容

新备份采用 schemaVersion 2，包含日记。仍可导入旧版 schemaVersion 1 备份。导入是完整替换：旧文件没有日记时，当前日记也会被清空。导入会替换当前记录，请保留原备份副本。

### 验证说明

74 项单元/集成测试、全部 17 项生产版本浏览器测试通过，TypeScript 与生产构建通过。具体检查与设备限制见 [QA.md](QA.md)。浏览器自动化不能验证手机实际振动。上一版本的任务开关触感已由用户确认；Safari 普通按钮仍使用视觉按压反馈。

## 1.1.1 — 2026-09-15

- 统一导航、操作按钮、折叠控件及选择控件的按压反馈；支持网页振动的浏览器可启用轻震。
- 子任务使用与任务相同的原生勾选控件，在支持的 iPhone 上接入系统触感。
- 增加回到前台、恢复网络时检查更新，以及 Check for updates 和版本号显示。
- 验证：52 项单元测试通过，专项浏览器检查通过。用户提供的 14 项浏览器通过结果对应此前的任务动画版本，不是此后这次更新的全套结果。

更早的部署尚未显示版本号，此处不事后编造版本编号。
