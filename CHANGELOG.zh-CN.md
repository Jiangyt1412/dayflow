# 更新记录

[English](CHANGELOG.md) · [打开 DayFlow](https://jiangyt1412.github.io/dayflow/)

## 1.2.0 — 2026-09-16

- Insights 第一项改为 Notes，可按日期记录日记、心情和状态，保存在本机并纳入 JSON 备份。
- Focus 移至第二项，移除原 Overview。
- 重做专注统计：今日、本周、本月和全部累计时长；以引导线直接连接分类名称、时长与扇区的饼图；可横向滚动的小时分布；本月逐日和全年 1–12 月折线图。
- 图表按已保存的实际专注片段分配到本地日期和小时，排除暂停；未结束的计时暂不计入。小时分布不再设置最低记录数量门槛。
- 专注历史和详情显示任务名称。新记录保存名称快照；删除仍存在的任务时，也为旧专注记录补存名称。
- 区分进食总记录次数与选定期间的每日平均；平均值包含没有记录的日期。
- 移除非交互页面容器的浏览器焦点外框，折叠按钮的点击高亮设为透明，保留按钮和输入框的键盘焦点提示。尚未复现用户反馈的 iPhone 偶发蓝线，不能确认其唯一来源。
- 新增按版本记录的更新日志，Settings → What’s new 可查看。

### 备份兼容

新备份采用 schemaVersion 2，包含日记。仍可导入旧版 schemaVersion 1 备份。导入是完整替换：旧文件没有日记时，当前日记也会被清空。导入会替换当前记录，请保留原备份副本。

### 验证说明

74 项单元/集成测试、全部 17 项生产版本浏览器测试通过，TypeScript 与生产构建通过。具体检查与设备限制见 [QA.md](QA.md)。浏览器自动化不能验证手机实际振动。上一版本的任务开关触感已由用户确认；Safari 普通按钮仍使用视觉按压反馈。

## 1.1.1 — 2026-09-15

- 统一导航、操作按钮、折叠控件及选择控件的按压反馈；支持网页振动的浏览器可启用轻震。
- 子任务使用与任务相同的原生勾选控件，在支持的 iPhone 上接入系统触感。
- 增加回到前台、恢复网络时检查更新，以及 Check for updates 和版本号显示。
- 验证：52 项单元测试通过，专项浏览器检查通过。用户提供的 14 项浏览器通过结果对应此前的任务动画版本，不是此后这次更新的全套结果。

更早的部署尚未显示版本号，此处不事后编造版本编号。
